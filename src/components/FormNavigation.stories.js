import FormNavigation from './FormNavigation';

export default {
  title: 'components/FormNavigation',
  component: FormNavigation,
  argTypes: {},
};

const Template = args => <FormNavigation {...args} />;

export const Default = Template.bind({});
Default.args = {};
